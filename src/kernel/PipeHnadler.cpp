#include "PipeHandler.h"

size_t PipeHandler::read(char* buffer, size_t offset, size_t length) 
{

	return 0;
}



size_t  PipeHandler::write(char* buffer, size_t offset, size_t length)
{

	return 0;
}